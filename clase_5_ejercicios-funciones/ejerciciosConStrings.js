// ejercicios con strings

// puedes usar los metodos de string vistos en clase

// ejemplo: declara una funcion que retorne el numero de caracteres que tiene una cadena de texto (longitud)
// declara tu funcion aqui 

let muestra = "tengo";

function longPhrase(str) {
    return str.length;
}

console.log(longPhrase(muestra));
// let mensaje = "si, tu puedes!!!";

// console.log(mensaje);
// console.log(mensaje.length);

function findLastLetter(text) {
    // escribe tu solucion
    
    return text
}